import java.io.IOException;

public class App {
	public static void main(String[] args) throws IOException {
		/**
		 * MEHMET KUTAY AKPINAR 300201121
		 * MERT KARAHAN 300201050
		 * 
		 * All class and interface implementations are taken from the textbook given to us and arranged.
		 * Using the list implementation in FileIO, we put all the things we read in a list. 
		 * Later, while adding to the waiting line(queue) and pile(stack), we adjusted this list
		 * according to the compare methods we wrote (ordering it according to the desired one) 
		 * and pushed it to the stack and enqueue the queue.
		 * 
		 */
		ProgramRun executionRunner=new ProgramRun();
		executionRunner.run();
		
	}
}
